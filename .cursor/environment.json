{
  "name": "1d-arc-popper",
  "user": "ubuntu",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "bash scripts/cloud-agent-install.sh",
  "terminals": [
    {
      "name": "eval",
      "command": "bash -lc 'source .venv/bin/activate 2>/dev/null || true; exec bash'",
      "description": "Shell with venv for Popper 1D-ARC train/test (do_train.sh / do_test.sh / results.py)."
    }
  ]
}
